set quiet

import 'just/dev.just'            # `just dev` — merged into root namespace

mod docs 'just/docs.just'        # docs::live/build/lint/validate/clean/image-build/image-run
mod version 'just/version.just'  # version::show/bump/tag
mod copier 'just/copier.just'    # copier::update/diff/recopy

# Default: list available recipes
default:
    just --list
